const route = require("express").Router();

const productController = require("../controllers/product");

// route.get("/search", movieController.handleSearch);
// route.get("/viewdetails", movieController.handleViewDetails);
// route.post("/import", movieController.handleImportJSON);
// route.get("/import", movieController.handleImport);
// route.get("/reviews", movieController.getReviewOfMovie);

module.exports = route;